<?xml version="1.0" encoding="utf-8"?>
<!--
  Placeholder shown while a markdown image loads. The <size> gives the drawable an
  intrinsic dimension so Markwon reserves vertical space up front, which limits the
  layout jump when the real image finishes loading. The translucent grey reads fine
  on both light and dark surfaces.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#22808080" />
    <corners android:radius="8dp" />
    <size android:width="320dp" android:height="180dp" />
</shape>
